<resources>
    <!-- Base application theme. -->
    <style name="Theme.CyberSecurity" parent="Theme.Material3.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/accent_color</item>
        <item name="colorSurface">@color/background</item>
        <item name="colorPrimaryVariant">?attr/colorSurface</item>

        <item name="colorOnPrimary">@color/white</item>

        <item name="android:statusBarColor">?attr/colorSurface</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/accent_color</item>
        <item name="colorSecondaryVariant">@color/accent_color</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="colorSecondaryContainer">@color/blue_bottom_nav</item>
        <!-- Status bar color. -->
        <!-- Customize your theme here. -->
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Custom.SplashScreen" parent="Theme.SplashScreen">
        <item name="backgroundColor">@color/screen_1</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_round</item>
        <item name="postSplashScreenTheme">@style/Theme.CyberSecurity</item>
    </style>
</resources>